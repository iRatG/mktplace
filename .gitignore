# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# Environment
.env
.env.local
.env.production
.env.prod
!.env.example

# Docker
.docker/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Coverage
htmlcov/
.coverage
.coverage.*
coverage.xml

# Pytest
.pytest_cache/

#user files
key_param
key_param.txt
tz.md

# QA-тестировщикам — рассылаются напрямую файлом, в репозиторий не попадают
/blog_test_1.html
/adv_test_2.html

#presentation
presentation*.*

# задания
uridic_review.md

#Домен
key_host_reg_ru.md

# Бизнес-переписки/задания и черновики писем — своя локальная git-история внутри task/, в общий репозиторий не попадает
task/

# OpenSpec-спецификации — рабочий инструмент для нас с ИИ, своя локальная git-история
# внутри openspec/, в общий репозиторий пока не попадает (см. openspec/config.yaml)
openspec/

# Claude Code: локальные настройки сессии — содержат историю разрешённых команд
# (в т.ч. с реальными паролями VPS, вставленными в сами команды) и runtime-файлы.
# .claude/commands/ и .claude/skills/ (интеграция OpenSpec) НЕ игнорируются намеренно.
.claude/settings.local.json
.claude/scheduled_tasks.lock